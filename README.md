# darkest_dungeon_docker
testing darkest dungeon in docker


Dockerfile, build.sh, runme.sh are for centos
Dockerfile.ubuntu, build.ubuntu.sh, runme.ubuntu.sh are for ubuntu

sets up, copies file to container, then I used to install DD manually by running installer
